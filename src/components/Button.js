import logo from '../assests/images/logo.svg';
import '../assests/styles/App.scss';

function Btn({ img, title, price }) {
  return (
    <div className="App">
        <h1>{title}</h1>
    </div>
  );
}

export default Btn;
